@model RedSocial.Core.Application.ViewModels.Users.ForgotPasswordViewModel;



<div class="container-fluid">

    <div class="row bg-dark">
        <div class="col-6 offset-3">
            <div class="card">
                <div class="card-header bg-dark text-light">
                    <h4>Olvide mi contraseña</h4>
                </div>
                <div class="card-body">

                    @if (Model.HasError)
                    {
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <p>@Model.Error</p>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }
                    <form method="post" asp-controller="Users" asp-action="ForgotPassword">

                        <div class="mb-3">
                            <label asp-for="Email" class="form-label">Correo: </label>
                            <input asp-for="Email" class="form-control" />
                            <span asp-validation-for="Email" class="text-danger"></span>
                        </div>

                        <button type="submit" class="btn btn-outline-dark text-black fw-bold">Enviar</button>
                        <a asp-controller="Users" asp-action="Index" class="btn btn-outline-dark text-black fw-bold">Volver atras</a>

                    </form>

                </div>

            </div>
        </div>
    </div>

</div>

